services:
  strokepredictor:
    image: singer_strokepredictor
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - INPUT_DIR=/data/input
      - OUTPUT_DIR=/data/output
    volumes:
      - ./data/input:/data/input:ro
      - ./data/output:/data/output